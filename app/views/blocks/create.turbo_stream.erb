<% if @insert_after %>
  <%= turbo_stream.after @insert_after, partial: 'blocks/block', locals: { block: @block } %>
<% else %>
  <%= turbo_stream.append dom_id(@page), partial: 'blocks/block', locals: { block: @block } %>
<% end %>
